apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: "testing-apps"
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/arfanragil/manifest.git
    targetRevision: main
    path:
  destination:
    server: https://kubernetes.default.svc
    namespace: staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 10
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
